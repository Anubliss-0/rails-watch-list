<h1>new</h1>
<%# create form to create lists %>
<%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.input :photo, as: :file %>
<%= f.submit %>
<% end %>

<%= link_to "Back to lists", lists_path %>
